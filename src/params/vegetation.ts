// Paramètres de la végétation.

export const FORET = {
  /** Limite de la forêt (m), plus basse en ubac, avec des bords irréguliers. */
  limite: 2250,
  ecartUbac: 170,
  irregularite: 140,
  /** Pas d'arbre au-delà de cette pente (%). */
  penteMax: 70,
  /** Marge (m) sans arbre autour de l'aire de la station. */
  lisiereStation: 60,
  /** Nombre d'arbres pour une carte de 200 unités de côté, et plafond absolu. */
  densite: 4600,
  arbresMax: 24000,
};
